#include "main.h"

/**
 * _islower - check for lower case character.
 * @c : The parameter c represents character to be checked.
 * Return: 1 for lowercase character or 0 for anything else.
 */

int _islower(int c)
{

	if (c > 96 && c < 123)
	{
		return (1);
	}

	return (0);

}

